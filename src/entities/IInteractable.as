package entities 
{
	
	/**
	 * An interface for objects that can be interacted with, such as
	 * NPCs, items, switches and computers.
	 * @author Zachary Lewis (http://zacharylew.is)
	 */
	public interface IInteractable 
	{
		function interact():void;
	}
	
}